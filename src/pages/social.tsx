import Shell from "@/components/shell";
import SociaList from "@/components/socialList";
import React from "react";

const Social = () => {
  return (
    <Shell>
      <SociaList />
    </Shell>
  );
};

export default Social;
